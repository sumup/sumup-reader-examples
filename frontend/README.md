# Frontend Examples

- `static/` - Plain HTML/CSS/JS version.
- `react/` - React + Vite version.

Both variants link readers via `POST /readers`, list them via `GET /readers`, and start a reader checkout via `POST /readers/{id}/checkout`.
